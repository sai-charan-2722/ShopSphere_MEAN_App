export const environment = {
  production: true,
  // These are replaced at build time on Vercel via environment variables / file replacement.
  apiUrl: 'https://shopsphere-backend.onrender.com/api',
  wsUrl: 'https://shopsphere-backend.onrender.com',
  clerkPublishableKey: 'pk_test_replace_me',
};
